# Active Packet

> The one packet currently in flight. The orchestrator reads this every session;
> the builder implements exactly this and nothing else; the archivist clears it
> on close. One packet at a time.

- **Status:** NONE ACTIVE
- **Last-closed:** P-028 — Source `doctrine_engine.py`'s producer constants from
  the reference profile (byte-identical, WIDENED — the last & largest extraction).
  Receipt: `build-os/receipts/P-028-doctrine-sources-values-from-reference-profile.md`.
  **This COMPLETES the EXTRACTION PHASE** of the producer-agnostic epic — the whole
  judgment layer (creative + governance + doctrine) is now sourced from the
  reference `ProducerProfile`, byte-identical, with the physics chassis + safety
  kill-switches left hardcoded. Finding A fully resolved; aliasing-proof discharged
  for all 3 modules.

## Next (staged — confirm active via the orchestrator before building)

- **P-029 — parameterize the pipeline by a per-call producer profile (THE PIVOT).**
  Thread a per-call `producer` through the pipeline so `analyze(producer=...)`
  SELECTS a profile — the profile stops being a mirror of today's hardcoded values
  and becomes a LIVE lever; the first SELECTABLE step. **Default = the
  `halee_ramone` reference, byte-identical** (guarded by the P-025 round-trip +
  the 68/68 regression). P-029 is ALSO the structural fix that ends the
  shared-module-singleton aliasing risk (the per-call profile removes the shared
  mutable global that P-026/P-027/P-028 each had to prove safe per-module).
  - Epic arc after P-029: P-030 (rename `halee`/`ramone` dims off producer names)
    → P-031 (confidence framework — consume the metadata stamp) → P-032 (second
    producer) → P-033 (expose producer selection).

---
_No packet in flight. The extraction phase is complete; the orchestrator-in-chief
opens the pivot (P-029) next. One packet at a time._
