# Active Packet

> The one packet currently in flight. The orchestrator reads this every session;
> the builder implements exactly this and nothing else; the archivist clears it
> on close. One packet at a time.

- **Status:** ACTIVE — P-032c confirmed by the orchestrator-in-chief
  (2026-07-01), handed to builder.

## Last-closed

- **P-032d — `rhythmic_surprise` (weak form). ✓ CLOSED** — qa GREEN, reviewer
  PASS (no must-fix; reviewer ran THREE own sabotages, all caught; Codex NOT
  available — single-model review). The FOURTH new producer-agnostic doctrine
  axis (cross-section transient-density variation: pstdev spread + largest
  adjacent swing), byte-identical for halee_ramone (weight 0; 33/33 pre-existing
  values × 3 fixtures unchanged). **11 component axes; 4 of the 7 Timbaland
  "weight up" axes landed.** Single commit `8a81516` (HEAD IS Commit-1 → green
  in isolation); suite 433 → 451; regression 68/68 UNCHANGED. Live fixture
  scores 51.1 / 20.0 / 27.8. Local-only, NOT merged (merge base `e79426a` =
  PR #16). Receipt: `build-os/receipts/P-032d-rhythmic-surprise.md`.

## Active packet spec (confirmed)

- **Packet id:** P-032c
- **Title:** `low_end_motion` (pocket) — fifth new producer-agnostic doctrine
  axis; byte-identical for halee_ramone (weight 0), per the resequenced order
  (d ✓ → **c** → g → f).
- **Key cautions (from the read-only scoping workflow, reconfirmed at P-032d
  close):**
  - **Pure additive, 5 in-arg inputs — ZERO new plumbing** (`score_doctrine`
    already receives everything it reads; P-032d confirmed the same claim in
    practice).
  - **Distinctness-vs-`_static_mix` needs care:** low_end_motion rewards a
    POSITIVE relationship to low-end movement whereas `_static_mix` applies a
    hygiene PENALTY — prove the axes orthogonal, not a re-derivation.
  - **Presence is a GATE only — "more bass" must NOT win.** Low-end presence
    gates the score; the score rewards MOTION, never sheer level.
  - **Honest deferrals (docstring, NOT faked):** kick/sub temporal interlock,
    low-end motif detection, per-section true-sub movement — all need
    onset/sequence signal not visible at doctrine time.
  - **Physics primary; identity labels only a corroborating tie-break.**
  - Established idiom: append `low_end_motion_score` LAST to `component_scores`
    (11-term summation order preserved), weight 0 in `halee_ramone.json`,
    constants block in `doctrine.scorers.low_end_motion`, `_validate`
    required-scorers, optional schema property; byte-identical (independent
    capture) + distinctness + liveness/sabotage + no-aliasing + honest-scope
    tests; run `fixtures/generate_fixtures.py` first; ≤2 commits, Commit-1
    green in isolation; full suite green from the 451 baseline; regression
    68/68.

## Epic arc (Timbaland sub-arc P-032.x — RESEQUENCED)

**P-032e ✓ → P-032a ✓ → P-032b ✓ → P-032d ✓ → P-032c (low_end_motion — ACTIVE)
→** P-032g (loop static-vs-iconic) → P-032f (vocal-role — ★ USER-GATED) →
**[fold P-031 confidence here]** → P-032h (author `timbaland.json`) → P-032i
(differential proof). P-030 (rename dims) orthogonal/last.

---
_Cleared by the archivist on P-032d close (2026-07-01). One packet at a time.
The orchestrator confirms the staged packet; the builder implements exactly it;
qa proves; reviewer judges; archivist closes with a receipt._
